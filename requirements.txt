# Telegram bot
aiogram==3.24.0
aiohttp==3.11.18
Babel==2.18.0

# System control / media
# Compiled packages use lower bounds so pip can grab a wheel matching the
# running Python (exact pins to older versions have no wheels on Python 3.14+).
psutil>=7.0.0
opencv-python>=4.11.0.86
pyttsx3==2.98
langid==1.1.6
pyperclip==1.9.0
requests==2.32.3
loguru==0.7.3
pywin32>=312

# Проигрывание присланных аудио и голосовых.
# libsndfile в soundfile 0.13+ умеет ogg/opus, в котором Telegram шлёт голосовые
sounddevice>=0.5.1
soundfile>=0.13.1

# GUI
customtkinter==5.2.2
CTkMessagebox==2.7
pystray==0.19.5
Pillow>=11.3
